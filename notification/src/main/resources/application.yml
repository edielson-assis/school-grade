# Spring settings
spring:
  application:
    name: notification-service
  config:
    import: 'configserver:'
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server